<Window
    x:Class="PGInstaller.DomainJoinWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Join Domain"
    Width="480"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    SizeToContent="Height"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None">

    <Border
        Margin="0"
        Background="{StaticResource SurfaceBrush}"
        BorderBrush="{StaticResource BorderBrush}"
        BorderThickness="1"
        CornerRadius="16"
        Effect="{StaticResource SoftShadow}">
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,24"
                FontSize="22"
                Style="{StaticResource PageTitleStyle}"
                Text="JOIN COMPANY DOMAIN" />

            <TextBlock
                Grid.Row="1"
                Margin="0,0,0,6"
                Style="{StaticResource CaptionStyle}"
                Text="DOMAIN NAME" />
            <TextBox
                x:Name="TxtDomain"
                Grid.Row="2"
                Margin="0,0,0,16"
                Style="{StaticResource ModernTextBox}" />

            <StackPanel Grid.Row="3" Margin="0,0,0,6" Orientation="Horizontal">
                <TextBlock Style="{StaticResource CaptionStyle}" Text="DOMAIN USER ACCOUNT" />
                <TextBlock
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="10"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Text="(e.g., username or DOMAIN\username)" />
            </StackPanel>
            <TextBox
                x:Name="TxtUsername"
                Grid.Row="4"
                Margin="0,0,0,16"
                Style="{StaticResource ModernTextBox}" />

            <TextBlock
                Grid.Row="5"
                Margin="0,0,0,6"
                Style="{StaticResource CaptionStyle}"
                Text="PASSWORD" />
            <PasswordBox
                x:Name="TxtPassword"
                Grid.Row="6"
                Margin="0,0,0,16"
                Style="{StaticResource ModernPasswordBox}" />

            <TextBlock
                x:Name="TxtStatus"
                Grid.Row="7"
                Margin="0,0,0,16"
                FontSize="11"
                Foreground="{StaticResource TextSecondaryBrush}"
                TextWrapping="Wrap"
                Visibility="Collapsed" />

            <Grid Grid.Row="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ProgressBar
                    x:Name="LoadingBar"
                    Grid.Column="0"
                    Margin="0,0,16,0"
                    VerticalAlignment="Center"
                    Foreground="{StaticResource PrimaryBrush}"
                    IsIndeterminate="True"
                    Visibility="Collapsed" />
                <Button
                    x:Name="BtnJoin"
                    Grid.Column="1"
                    Width="110"
                    Height="44"
                    Margin="0,0,12,0"
                    Click="BtnJoin_Click"
                    Content="JOIN"
                    IsDefault="True"
                    Style="{StaticResource PrimaryButton}" />
                <Button
                    x:Name="BtnCancel"
                    Grid.Column="2"
                    Width="110"
                    Height="44"
                    Click="BtnCancel_Click"
                    Content="CANCEL"
                    IsCancel="True"
                    Style="{StaticResource SecondaryButton}" />
            </Grid>
        </Grid>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Window.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation
                            Storyboard.TargetProperty="Opacity"
                            From="0"
                            To="1"
                            Duration="0:0:0.3" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
    </Border>
</Window>
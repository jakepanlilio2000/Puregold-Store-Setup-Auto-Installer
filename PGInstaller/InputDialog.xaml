<Window
    x:Class="PGInstaller.InputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Configuration"
    Width="450"
    Height="AUTO"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None">

    <Border
        Background="{StaticResource SurfaceBrush}"
        BorderBrush="{StaticResource BorderBrush}"
        BorderThickness="1"
        CornerRadius="16"
        Effect="{StaticResource SoftShadow}">
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Question Text  -->
            <TextBlock
                x:Name="TxtQuestion"
                Grid.Row="0"
                Margin="0,0,0,16"
                FontSize="18"
                Style="{StaticResource PageTitleStyle}"
                Text="Configuration"
                TextWrapping="Wrap" />

            <!--  Input Field  -->
            <TextBox
                x:Name="TxtAnswer"
                Grid.Row="1"
                Margin="0,0,0,24"
                Style="{StaticResource ModernTextBox}" />

            <!--  Action Buttons  -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!--  IsDefault="True" allows pressing Enter to trigger this button  -->
                <Button
                    x:Name="BtnOk"
                    Grid.Column="1"
                    Width="100"
                    Height="40"
                    Margin="0,0,12,0"
                    Click="BtnOk_Click"
                    Content="OK"
                    IsDefault="True"
                    Style="{StaticResource PrimaryButton}" />

                <!--  IsCancel="True" allows pressing Esc to trigger this button  -->
                <Button
                    x:Name="BtnCancel"
                    Grid.Column="2"
                    Width="100"
                    Height="40"
                    Click="BtnCancel_Click"
                    Content="CANCEL"
                    IsCancel="True"
                    Style="{StaticResource SecondaryButton}" />
            </Grid>
        </Grid>

        <!--  Smooth Fade-in Animation  -->
        <Border.Triggers>
            <EventTrigger RoutedEvent="Window.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation
                            Storyboard.TargetProperty="Opacity"
                            From="0"
                            To="1"
                            Duration="0:0:0.3" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
    </Border>
</Window>